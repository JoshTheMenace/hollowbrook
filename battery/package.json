{
  "name": "battery",
  "private": true,
  "type": "module",
  "description": "Readiness-campaign micro-game battery: each entry stresses one pipeline seam, full contract->gates->review pass",
  "scripts": {
    "dev": "vite"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
